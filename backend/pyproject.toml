[project]
name = "chart-volume-backend"
version = "0.1.0"
description = "Crawl + Wyckoff analysis backend for VN30 stocks"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q"
filterwarnings = ["ignore::DeprecationWarning"]
