# Only needed to run the renderer in a plain browser (npm run dev:renderer) WITHOUT Electron.
# Inside Electron, apiBase + token are injected by the preload bridge.
VITE_API_BASE=http://127.0.0.1:8787
VITE_API_TOKEN=change-me
